Search Wikipedia for: $ARGUMENTS

Use WebSearch with `site:wikipedia.org` to find the most relevant article(s). Use WebFetch to read the full Wikipedia page(s) — extract the summary, key sections, and any linked concepts worth following. Use Wikipedia primarily for definitions, entity resolution, structured overviews, and finding related topics to explore further.

Return the key facts, definitions, and related topics found, with source URLs.
